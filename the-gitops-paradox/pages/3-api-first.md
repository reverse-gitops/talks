# Why an API?

Let's start first: why an API? Isn't git an API?

Wel let's start in the past: a file is certainly an API. Back in the 1960 unix invented it, and it's beatiful.

So why something else? Because people need more than only files: most of them like to click arround. We hate that, but they dont.

---

# The briliant API designed

Back in the day when Kuberntes was designed it became quite apperent that everything for doing an Kubernetes API call could always be reflected in a single YAML file.

Let that sync in. A pure REST API disucssion would probably quickly throw the idea out of the window: but it's actually a very coo lproperty that forms the basis of reverse-gitops.

(Get an example that shows left an API call with curl and on the let an yaml file with the same content and a kubectl apply -f)

---


